{
    "main": "dist/index.js",
    "dependencies": {
        "moment": "2.29.4"
    },
    "devDependencies": {
        "@types/node": "^12.12.5",
        "typescript": "^3.6.4",
        "rimraf": "^3.0.0",
        "npm-run-all": "^4.1.5",
        "aws-sdk": "^2.466.0"
    },
    "scripts": {
        "clean": "rimraf dist/*",
        "compile": "tsc",
        "build": "npm-run-all clean compile"
    }
}
